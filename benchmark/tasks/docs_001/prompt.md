`text/segment.py` contains a fully documented function with an empty body — the docstring specifies its contract, including the edge cases, but nobody ever wrote the implementation.

Implement it to match the documented contract exactly.

Requirements:

- The docstring is the specification. Every stated behaviour, including the edge cases, must hold.
- Do not modify the docstring or the signature.
- Write tests derived from the documented behaviour.
- Prefer clarity over cleverness — this function will be read often.
