A fresh clone of this repo can't be built. Following the setup steps in the README fails partway through, and nobody remembers what state their laptop is in that makes it work locally.

Get the build working from a clean checkout.

Requirements:

- The build command documented in the README must succeed end to end.
- Fix the project configuration rather than documenting a manual workaround.
- If the README instructions themselves are wrong, correct them.
- Use the terminal to verify — don't assume a fix worked, run it.
