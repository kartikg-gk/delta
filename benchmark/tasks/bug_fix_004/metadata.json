{
  "id": "bug_fix_004",
  "title": "State leaks between requests",
  "difficulty": "Medium",
  "tags": [
    "bug-fix",
    "debugging"
  ],
  "capability": "Diagnosing a shared-mutable-state bug that only manifests on the second call. Tests the agent's willingness to run the code twice instead of reading it once.",
  "verification": "verify.py runs a test that invokes the handler twice in one process and asserts the second result is independent of the first.",
  "prompt_file": "prompt.md",
  "repo_dir": "repo",
  "verifier": "verify.py",
  "timeout_seconds": 1200,
  "allowed_tools": [
    "Read",
    "Write",
    "Edit",
    "Bash",
    "Grep",
    "Glob"
  ]
}
