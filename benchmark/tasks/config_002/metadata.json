{
  "id": "config_002",
  "title": "Environment-driven configuration with precedence",
  "difficulty": "Medium",
  "tags": [
    "config",
    "feature"
  ],
  "capability": "Implementing layered configuration precedence correctly, including the boundary cases (empty string vs unset, type coercion) that naive implementations get wrong.",
  "verification": "verify.py exercises every layer combination and asserts the documented precedence, including unset-vs-empty and boolean coercion of values like '0' and 'false'.",
  "prompt_file": "prompt.md",
  "repo_dir": "repo",
  "verifier": "verify.py",
  "timeout_seconds": 1200,
  "allowed_tools": [
    "Read",
    "Write",
    "Edit",
    "Bash",
    "Grep",
    "Glob"
  ]
}
