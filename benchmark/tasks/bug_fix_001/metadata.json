{
  "id": "bug_fix_001",
  "title": "Broken imports after a refactor",
  "difficulty": "Easy",
  "tags": [
    "bug-fix",
    "debugging",
    "navigation"
  ],
  "capability": "Baseline loop competence: read a traceback, follow it to the real file, make a minimal edit. Exercises Read + Bash(pytest) + Edit in the shortest possible tool cycle.",
  "verification": "verify.py runs `pytest -q` in repo/ and asserts exit code 0, then asserts `git diff --stat` touches no file under repo/tests/.",
  "prompt_file": "prompt.md",
  "repo_dir": "repo",
  "verifier": "verify.py",
  "timeout_seconds": 600,
  "allowed_tools": [
    "Read",
    "Write",
    "Edit",
    "Bash",
    "Grep",
    "Glob"
  ]
}
