Our README has drifted. Several of the examples are from an older API and at least one of the documented commands no longer exists. New users hit this on day one.

Bring the documentation back in line with the code.

Requirements:

- Every command and code example in the README must actually run against the current code — verify each one by executing it rather than reading it.
- Where the documentation is right and the code is wrong, say so in your summary rather than silently changing the docs to match a bug.
- Don't rewrite the structure or tone; correct what's inaccurate.
