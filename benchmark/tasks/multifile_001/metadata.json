{
  "id": "multifile_001",
  "title": "Rename a core type across the codebase",
  "difficulty": "Medium",
  "tags": [
    "multi-file",
    "refactor",
    "navigation"
  ],
  "capability": "Exhaustive search-and-replace where naive text replacement fails: the old name appears as a substring of unrelated identifiers, in strings, and in serialised fixtures that must NOT change.",
  "verification": "verify.py asserts the suite passes, that the old identifier is gone from source, and that the on-disk JSON fixtures still contain the original serialised key names.",
  "prompt_file": "prompt.md",
  "repo_dir": "repo",
  "verifier": "verify.py",
  "timeout_seconds": 1200,
  "allowed_tools": [
    "Read",
    "Write",
    "Edit",
    "Bash",
    "Grep",
    "Glob"
  ]
}
